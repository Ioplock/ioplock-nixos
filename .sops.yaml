keys:
  # Replace this placeholder with the host's age recipient.
  # Run `nix run .#sops-init-recipient` on the host after first boot to derive it
  # from /etc/ssh/ssh_host_ed25519_key.pub via ssh-to-age, then re-encrypt
  # secrets/sing-box-outbounds.json with the real recipient.
  - &acrux age1vjjve4xm7a8gc2es5yelngpn8yjskfptd7ahyjr4e3k7p0kawylsphwazy
creation_rules:
  - path_regex: secrets/.*\.json$
    key_groups:
      - age:
          - *acrux
